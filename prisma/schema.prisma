// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Users {
  id          Int          @id @default(autoincrement())
  firstName   String
  lastName    String
  age         Int?
  email       String       @unique
  password    String
  avatar      String?
  acceptTerms Boolean      @default(false)
  isOnline    Boolean      @default(false)
  createdAt   DateTime     @default(now())
  tokens      UserTokens[] @relation(name: "UserTokensToUsers")
  messages    Messages[]   @relation(name: "MessagesToUsers")
  roomUsers   RoomUsers[]  @relation(name: "RoomUsersToUsers")
}

model UserTokens {
  id               Int      @id @default(autoincrement())
  refreshTokenHash String
  user             Users    @relation(name: "UserTokensToUsers", fields: [userId], references: [id])
  userId           Int
  createdAt        DateTime @default(now())
}

model RoomUsers {
  id     Int   @id @default(autoincrement())
  room   Rooms @relation(name: "RoomUsersToRooms", fields: [roomId], references: [id])
  roomId Int
  user   Users @relation(name: "RoomUsersToUsers", fields: [userId], references: [id])
  userId Int

  @@unique([roomId, userId])
  @@index([roomId])
  @@index([userId])
}

model Rooms {
  id        Int         @id @default(autoincrement())
  createdAt DateTime    @default(now())
  messages  Messages[]  @relation(name: "MessagesToRooms")
  roomUsers RoomUsers[] @relation(name: "RoomUsersToRooms")
}

model Messages {
  id        Int      @id @default(autoincrement())
  text      String
  isRead    Boolean  @default(false)
  createdAt DateTime @default(now())
  room      Rooms    @relation(name: "MessagesToRooms", fields: [roomId], references: [id])
  roomId    Int
  user      Users    @relation(name: "MessagesToUsers", fields: [userId], references: [id])
  userId    Int
}

model Terms {
  id        Int      @id @default(autoincrement())
  title     String   @db.VarChar(100)
  text      String   @db.Text
  createdAt DateTime @default(now())
}
